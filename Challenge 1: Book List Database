/*
Challenge COMPLETED: Book list database

Step 1:
Create a table to store a list of books. It should have columns for id, name, and rating. 
Add three of your favorite books into the table.

*/

CREATE TABLE books (id INTEGER PRIMARY KEY, name TEXT, rating INTEGER);

INSERT INTO books VALUES (1, "Tigers Today", 5);
INSERT INTO books VALUES (2, "Swans Speed", 1);
INSERT INTO books VALUES (3, "Lizzy the Lizard", 4);

SELECT * FROM books;
